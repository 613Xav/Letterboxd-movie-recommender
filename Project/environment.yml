name: letterboxd
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - psycopg2           # ← psycopg v3 from conda-forge (prebuilt)
  - scikit-surprise
  - libpq             # optional but nice to pin the client lib explicitly
  - ca-certificates   # good hygiene in containers
  - pip:
      - aiohttp
      - beautifulsoup4
      - lxml
      - pandas
      - openpyxl
      # (keep any other pip-only deps here)
